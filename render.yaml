services:
  - type: web
    name: langchain-chatbot-groq
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run chatbot/app.py --server.port=$PORT --server.address=0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11